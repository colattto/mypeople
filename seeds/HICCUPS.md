# mypeople SEED — in-container engineer hiccup ledger (Rule 27)

Systematic per-run collection of where **blind generation missed** / what each in-container
engineer had to fix beyond the seed. Every entry must be **folded back** into the seed (cite the §)
or marked OPEN. Harvest sources per run: each gen agent's transcript + pane, its agent-wiki, and
the Verify failures/retries it hit before exit 0.

**Status of collection before 2026-06-17:** NOT systematic. Hiccups were folded ad-hoc from MY
driving observations, not harvested per in-container engineer. This ledger + `harvest-hiccups.sh`
close that gap going forward; the table below backfills what is recoverable.

| Run | Hiccup (what blind gen missed / engineer had to fix) | Folded? → § |
|---|---|---|
| install-flow (pre-gen) | Step-2 `pkill` killed the driver's queue-client; Verify cleanup killed `main:Boss` → node dropped off central | ✅ §5.11 two-plane isolation + §5.3 |
| install-flow | `docker exec … bash -s` missing `-i` → heredoc never reached stdin (no-op install) | ✅ driver (`install-seed.sh`) |
| install-flow | block extractor skipped an indented ```bash block → Step 9 ran twice → "Address already in use" / stale pidfile | ✅ §5.9 liveness/self-heal + driver dedent-walk |
| round-1 | bypass-permissions + trust-folder consent dialogs; arrow keys typed as literal `^[[A` | ✅ launcher pre-accepts `~/.claude.json` + `--settings skipDangerous` |
| round-1 | seedrec recorded 0 segments — Playwright Chromium not installed | ✅ seedrec Step 2 `playwright install chromium` |
| round-2 | `purpose=None` on central — Mac central ran OLD code lacking the field | ✅ → fleet-hub design; §4 heartbeat carries `purpose` |
| round-2 | self-contained install pointed queue-client at 127.0.0.1 → node left the central grid (invisible) | ✅ §5.11 OUTER uplink always re-announces to central |
| uplink | outer uplink registered LATE (after generation) → N nodes invisible while hydrating | ✅ §5.11 UPLINK-FIRST (register `state=hydrating` at bring-up) |
| batch-of-5 | monitor false-green: grep matched `SEED_RESULT=DONE` / `BLOCKED=reason` in the prompt-echo | ⚠️ driver-side (node-state monitoring + `grep -vE`); not a seed gate |
| batch-of-5 | generated TODO UI is a functional **look-alike**, not our product (823→131 lines) | ✅ §A canonical UI asset + checksum gate (J14) |
| CEO 2026-06-17 | generated tmux is generic, not his Dracula+TUI-fix config | ✅ §A.1 `~/.tmux.conf` asset + gate (J14) |

| batch 2026-06-17 (mpgen3 reset) | blind agents IGNORED "copy verbatim" and re-generated their own UI/tmux look-alikes (todos 159 vs 820 lines, tmux 21 vs 43), then self-reported `ready` → FALSE GREEN | ✅ §12 Step 3.5 (literal cp, NOT generate) + J14 (disk-sha, "if you generated your own, you FAILED") + external operator gate |
| batch 2026-06-17 | secret-injection token was an UNSPECIFIED fork — servers replace `__INJECT_SECRET__`, canonical todos used `__QUEUE_SECRET__`, authored HUD used `?s=` → 3 incompatible tokens, parity impossible | ✅ §A secret-injection contract: token pinned `__INJECT_SECRET__`, J14 compares ON-DISK (not served) bytes |
| batch 2026-06-17 (method) | "reset-to-pristine" reused containers — a SHORTCUT, not a true fresh-from-0 hydrate (Rule 14/28: reused container/auth can carry hidden state → false pass) | ✅ batch driver now TEARS DOWN + `docker run` fresh per node + fresh per-node device-login (no auth/container reuse) |

| batch 2026-06-17 (fresh mpgen4) | pinning the UI surfaced a SEED CONTRACT BUG: the pinned `todos.html` sends `doneCondition/dependsOn/workToDone/hardGate/state` (+ `add{text,parent}` subtasks), but §6/§A.2 documented `cond/deps/status/done` — gates J16/J17/J23 tested the WRONG field names → false-green risk. (mpgen4-5 caught it: "I'll make the server accept both name sets.") | ✅ §6 + §A.2 F3/F4/F5/F13 + J16/J17/J18/J23 corrected to the REAL names; "pinned todos.html is source of truth, verify against it not prose" |

| test-method 2026-06-17 | substrates registered to mpgen2-1, a PRE-EXISTING hub from a prior generation — a real fresh-from-zero user has NO hub, so J12/J13 only passed because of a survivor container → CONTAMINATED test. Seed also marked UPSTREAM **Required:yes**, making the standalone product (the actual fresh install) untestable. | ✅ §10 UPSTREAM now optional (standalone default); §5.11 + Step 1 + J12/J13 guarded FLEET-MODE-ONLY (skip when no upstream); §14 canonical test = SINGLE standalone node, nothing pre-existing |

| batch 2026-06-17 (fresh mpgen4, UNANIMOUS 3/5) | `claude --dangerously-skip-permissions` shows a SECOND "Bypass Permissions mode — Yes, I accept" dialog beyond §5.5 onboarding; the onboarding paste's Enter hit "No, exit" → killed the FIRST Boss. Persisted-accept flag is opaque. | ✅ §5.5: `mp spawn` wait-ready auto-detects+dismisses bypass (send `2`+Enter) + trust dialog BEFORE pasting |
| batch 2026-06-17 (fresh mpgen4) | multi-line bracketed paste renders as `[Pasted text #1]`; single Enter doesn't reliably submit (I hit this manually on mpgen3-3 too) | ✅ §5.5b: `mp send`/`spawn` sends Enter, waits ~0.4s, sends a 2nd Enter |
| batch 2026-06-17 (fresh mpgen4) | Stop hook fires before the transcript flushes → empty summary | ✅ §4: Stop hook retries ~4×/0.5s + falls back to session_id transcript lookup |
| batch 2026-06-17 (fresh mpgen4) | field-name bug did NOT trip a gate — agents wrote BOTH server + self-Verify with their own (consistent) names → false-green confirmed; only an EXTERNAL/seed-pinned gate catches it | ✅ already folded (§6 real names); next batch's gates test the pinned names |

### Batch results log
- **mpgen3 (reset-in-place, INVALID method):** all regenerated UI/tmux → parity ✗✗✗. Caught by external gate.
- **mpgen4 (fresh containers, fleet-mode, CONTAMINATED by pre-existing hub + pre-fix seed):** asset parity ✓✓✓ + reorder-gone ✓ on all 5; all 5 self-report J1–J25 pass. Does NOT count (J12/J13 hub-dependent; field-names pre-fix). Yielded the 3 findings above.
- **mpgen5 (STANDALONE, GENUINE — nothing pre-existing, hub destroyed, UPSTREAM unset, fixed seed): ✅ CONVERGED.** 5/5 fresh-from-zero nodes, externally verified (operator, not self-report): disk==canonical (todo✓ dash✓ tmux✓) ×5, reorder-gone ×5, serve 200/200 ×5, Boss-alive-on-own-HUD ×5, and an external feature probe (add · set `state` · `doneCondition` · comment · del — REAL field names) passed ×5. This is the valid one-shot a real user gets. (mpgen5-2 printed no DONE sentinel but is objectively clean on every external gate → pass per "trust the pane, not the self-report".)

| mpgen5 batch (per node, Rule-27 harvest — done late, CEO caught the miss) | **mpgen5-1:** J2 onboarding-summary OVERWRITE by Stop hook (real, durability), mp wait-ready string, setsid pidfile under job-control, J25 revive idempotency across re-runs, UTF-8 glyphs in pinned HTML break plain `grep` in Verify. **mpgen5-3:** tailscaled pre-started (§5.6), J2 onboarding-summary VOLATILITY (must be durable), Boss refused an injection-shaped onboarding prompt, first-run dialogs as warned. **mpgen5-2/-4/-5:** no HICCUPS.local written. | ✅ J2 durability folded; tailscaled-pre-start already §5.6; first-run dialogs already §5.5 |
| CEO live HUD 2026-06-17 (gates MISSED) | (1) `hydrating` state label had a zoom/pulse ANIMATION — ugly | ✅ removed all `animation:`/`@keyframes` from both pinned assets (re-pinned: todos `dac5…`, dash `8f9f…`); J29 no-animation gate |
| CEO live HUD 2026-06-17 (gates MISSED) | (2) grid showed TEST/DEMO fixtures (alpha/beta/retiredtest groups, dead hydrating phantoms) left by un-cleaned J11/J25 probes | ✅ J11/J25 must isolate+cleanup; §5.9 queue-server expires stale clients; J26 fresh-grid-is-clean gate |
| CEO live HUD 2026-06-17 (gates MISSED) | (2b) ATTACH link literal `http://x:7681` — unsubstituted placeholder host | ✅ J27 attach-host-is-real gate (never `x`/`100.0.0.0`/empty; matches tailnet IP) |
| CEO live node 2026-06-17 (J14 FALSE GREEN) | (3) RUNNING tmux ≠ his style despite J14-on-disk pass: live server had WheelUpPane BOUND, base-index 0, history 2000, no Dracula — server started before conf placed + TPM/dracula never installed | ✅ §A.1: place conf before server / source-file + install TPM+dracula; J28 gates the RUNNING server config, not just the file |

| CEO 2026-06-17 | unused features to CUT: subtasks, dependencies (blocked-by), hard-gate | ✅ removed from pinned `todos.html` (CSS + `childrenOf`/`unmetDeps` + card relations panel + indicators; re-pinned todos `635b…`, node --check OK) AND from contracts (§6: no `add{parent}`/`parent`/`dependsOn`/`hardGate`); J23 repurposed to a NEGATIVE gate (assert ABSENT); F13 row struck |

| CEO DECISION B 2026-06-17 | exact-pinned-UI (~59% code, fails the adversarial ≤10% bar) REJECTED in favor of truly-generative (almanac/teleprompter level, ≤10% code) | ✅ RE-ARCHITECTED: un-pinned `todos.html`/`dashboard.html` (deleted shipped assets, removed cp/sha256, dropped J14 checksum gate); §7+§A.2 now NL contracts + PLOW **design tokens/consts** (the agent GENERATES components from them); §A.3 verification is behavioral (J9 PLOW + J29 no-anim + J23 no-cut-features + F-gates), not byte-identity. Adversarial gate: seed = **0% pasted code** (0 code fences), ≤10% PASS. |
| CEO DECISION B — tmux tension | his exact `~/.tmux.conf` (verbatim file) vs the no-paste/≤10% bar | ✅ RESOLVED: ship his **settings as consts** in §A.1 (Dracula plugins, base-index 1, history 50000, wheel-unbound, copy-pipe-and-cancel) — small, counts as acceptable spec; the agent GENERATES the conf from them + installs TPM/Dracula + source-file; fidelity gated on the RUNNING server (J28), not a checksum. |

| mpgen7 batch (per node, Rule-27) — UNANIMOUS | TPM clones `dracula/tmux` into `~/.tmux/plugins/**tmux**` (repo basename), NOT `/dracula` — a dir-name check false-negatives | ✅ J28 reworded: verify Dracula by the RUNNING status bar, not a fixed dir |
| mpgen7 batch | Boss onboarding-summary durability (J2) — agents PINNED the summary so the Stop hook can't clobber it (the mpgen5 fix, now applied + confirmed) | ✅ already folded (J2); confirmed working |
| mpgen7 misc | tailscaled pre-running (§5.6 ✓); `claude --append-system-prompt-file` absent in 2.1.168; `bypassPermissionsModeAccepted` IS a top-level key on this image (varies) | noted; non-blocking |

### Batch results log (cont.)
- **mpgen7 (GENERATIVE, Decision B — truly fresh, nothing pre-existing, ≤10% seed): ✅ CONVERGED.** 5/5 nodes, externally verified: serve 200/200, PLOW tokens, NO animations, NO subtasks/deps/hard-gate/reorder, clean grid (only self), attach=real tailnet IP, Boss-on-own-HUD, **RUNNING tmux = his Dracula style** (base-index 1 / wheel-unbound / dracula), feature probe (add·set `state`·`doneCondition`·comment·del) green, SEED_RESULT=DONE. Adversarial ≤10% gate: PASS (0% pasted code).

| CEO browser-QA 2026-06-18 (SECURITY) | the generated TODO page shipped the QUEUE_SECRET in client JS (my own `__INJECT_SECRET__` contract put it in the page → readable by anyone who loads it) | ✅ §5.12 contract: secret server-side ONLY; browser auths via httpOnly session cookie (same-origin), gated endpoints accept cookie OR `X-Queue-Secret` (machines); removed `__INJECT_SECRET__` everywhere (§4/§6/§A); J30 (no secret in any served bytes + cookie-auth works without a client secret) + J31 (browser-QA pass) |

| CEO test 2026-06-18 | board→Boss ping fired on CREATE but not on COMMENT | ✅ §6: ping on add AND every `/todo/comment` (exempt only Boss's own); F14 + J3 + J32 |
| CEO test 2026-06-18 | no hot-reload — needed manual refresh to see new comments/responses | ✅ §7.2 LIVE-updates contract (poll ≤3s / SSE, preserve input) + F23 + J33 |
| CEO test 2026-06-18 | E2E comms loop didn't close; engineer batched questions | ✅ §8: Boss spawns engineer on ping, engineer posts to TODO **one-per-turn** (not batched), CEO comment-ping drives next round; F24 + J32 (joke protocol) |
| CEO test 2026-06-18 | generated TODO UX very different from production + many frontend errors | ✅ §7 production-quality contract (match `127.0.0.1:9933` feature-completeness, zero console errors, no dead controls); J31 browser-QA strengthened to catch console errors/dead controls; also fixed §7.1 "pulsing" contradiction (J29 = no animation) |

## OPEN (not yet folded)
- (none — 4 CEO fixes folded (comment-ping, hot-reload, E2E joke-loop, UI quality) + security fix; run a fresh fleet with J32/J33 + a real joke-protocol browser-QA.)

## Mechanism (per run, going forward)
Run `seeds/harvest-hiccups.sh <node...>` after each gen batch. It pulls, per node: Verify
retry/FAIL lines, the gen agent's wiki, and any divergence the agent self-noted; appends a dated
block here for triage. Triage = fold into the seed (cite §) or mark OPEN. A run is not "clean"
until its harvested hiccups are all folded.

## HARVEST 2026-06-17T16:45Z
### mpgen-1
- verify FAIL/retry lines:
- gen agent wiki tail:
- self-noted divergences (NOTE:/HICCUP:/had to):
### mpgen3-1
- verify FAIL/retry lines:
- gen agent wiki tail:
- self-noted divergences (NOTE:/HICCUP:/had to):
_triage: fold each into the seed (cite §) or move to OPEN above._

## HARDEN 2026-06-18 — fresh mpsub-2-inner (4 folds + Hydration removed), independent verify
- **J1–J30: 26/26 PASS, ZERO live patches** — confirmed INDEPENDENTLY: J2c (8 doctrine keywords), J21 (unread 0→2), J11 (HUD has NO machines/hydration grid — only agents+retired, §7.1 removal works on a blind generate), J23 (backend rejects banned keys), J27/J28/J30 (tmux+security+attach), J10/J27 tailnet via pre-provisioned authkey (NON-interactive, zero browser). The 4 prior root-cause folds + the Hydration removal all hold first-try.
- **J31/J33 FAILED first-try on the blind generate → worker LIVE-PATCHED (NOT zero-patch):** two NEW gaps surfaced:
  1. `/favicon.ico` 404 → browser auto-request → console error → J31 fail. → FOLD §5.13: servers MUST serve `/favicon.ico` 204.
  2. transient cookie-401 → `GET /` did NOT `Set-Cookie`, so the page's first `/todo/board` fetch was cookieless → 401 console error. → FOLD §5.12: page GET MUST Set-Cookie the httpOnly session.
  J31 strengthened to assert both. → a NEXT fresh hydration must pass J31 zero-patch.
- J32 (joke loop): worker reported pass; independent re-confirm pending (verify.sh timed out at J30).

| CEO 2026-06-18 (folder-trust) | the Claude Code "Accessing workspace … trust this folder?" dialog re-triggers + BLOCKS a spawned agent when it opens a NEW cwd (e.g. .../run/eng) not yet trusted — pre-accepting only the MAIN cwd isn't enough (trust is keyed per exact path) | ✅ §5.5c: pre-accept `projects[$HOME]/[$INSTALL_DIR]/[run subtree].hasTrustDialogAccepted` (merge, preserve keys) + `mp spawn` pre-trusts the EXACT --cwd before launching + bake into golden-image ~/.claude.json snapshot; J35 gates a spawn in a fresh subfolder (no trust/onboarding prompt, no block) |

## HARDEN 2026-06-18 #2 — fresh mpsub-2-inner (idle-model + J34 focus + prior folds), independent zero-patch verify
- **27/30 PASS FIRST-TRY, ZERO live patches** — confirmed independently: idle-model (J17 born-idle+enum+needs_brainstorm-rejected, J19 idle→working no gate, J20 brainstorm-gate-gone) ✅, J2 (summary kw=6), J21 unread, J23 (no parent/deps/hardgate), J27/J28 (tmux his), J29/J30 (no-anim+no-secret), favicon §5.13 (204), Hydration-removed (no grid), J32 joke-loop (2 distinct jokes + pings), J10 tailnet via authkey (non-interactive). The idle-model fold + Hydration removal + the 4 prior folds all hold first-try.
- **3 FAIL: J31/J33/J34 (browser-QA gates).** ROOT CAUSE (conclusive via curl, puppeteer-independent): the blind generate did NOT implement §5.12 "page GET Set-Cookie" — `curl -sI GET /` shows **Set-Cookie=0** → page's first /todo/board fetch is cookieless → transient 401 → browser console error → the 3 browser gates fail. (My independent puppeteer also couldn't fully run → empty probe values, but Set-Cookie=0 is the real gap.) → NOT a clean zero-patch one-shot.
- FOLD: §5.12 hardened to an imperative HARD requirement (was a soft blockquote the blind generate skipped) + **J30 now has a HEADER-LEVEL `curl -I` Set-Cookie gate** so the gap is caught without a browser → a blind generate cannot pass while missing it. Next fresh run should clear J31 zero-patch.

| fresh mpsub-2-inner #3 (substrate-flow) | tailscale MagicDNS hijacked /etc/resolv.conf (→100.100.100.100) → public DNS (github/npm/apt) broke mid-hydration | ✅ §5.6: `tailscale up --accept-dns=false` + resolver fallback (8.8.8.8/1.1.1.1); tailnet IP reach doesn't need MagicDNS |
| fresh mpsub-2-inner #3 (substrate-flow) | folder-trust dialog hit again on the inner | ⚠ §5.5c already folded — but the GOLDEN IMAGE snapshot isn't rebuilt with the trust-bake yet, so it manifested; ACTION: rebuild image ~/.claude.json snapshot (or mp-spawn pre-trust covers it) |
| fresh mpsub-2-inner #3 | §5.12 Set-Cookie STRENGTHENED contract → blind generate implemented it FIRST-TRY (self-check curl Set-Cookie=1) ✅ | the inline self-checkable contract worked where the soft blockquote didn't |

| CEO 2026-06-18 (folder-trust — ARCHITECTURE error caught) | the folder-trust unblock was done at the SUBSTRATE level (golden-image bake + manual ~/.claude.json on the inner) → a VANILLA user's fresh install would still trip the Boss-spawn trust dialog = defective. §5.5c was in the product seed as a contract but the blind generate didn't implement it, AND J35 didn't reset trust so the image bake MASKED the gap. | ✅ Re-folded into the PRODUCT: §5.5c made the install + generated `mp spawn` the OWNER of trust pre-accept (self-checkable, "do NOT assume substrate/image"); §12 Step 3 install writes the trust pre-accept itself; J35 now WIPES `projects` trust first (vanilla) so it proves the product self-handles, not the image. Image bake demoted to OUR-harness-only. |

| CEO 2026-06-18 (Boss attach → "can't find window: Boss") | HUD/TODO build attach `…?arg=-t&arg=mc-main:Boss` = `tmux attach -t mc-main:Boss` (resolves window BY NAME), but the spawned Boss window in `mc-main` is NOT named `Boss` — either mp-spawn omitted `-n <tab>`, or tmux `automatic-rename`(default ON) renamed it to the foreground cmd (`node`/`claude`). J8 was FALSE-GREEN: it checked only ttyd HTTP 200, but `tmux attach` fails INSIDE the pane (HTTP still 200). | ✅ Folded into PRODUCT seed: §mp-spawn now mandates `-n <tab>` on new-session/new-window + `automatic-rename off`/`allow-rename off` + re-assert name, with a self-check (`list-windows … grep -qx Boss` ⇒ WINDOW_OK). J8 hardened: must prove the attach target window RESOLVES (`list-windows -t mc-main … grep -qx Boss` + pane has no "can't find window"), not just 200. To confirm actual window name on fresh eng-4 inner Boss. |

| CEO 2026-06-18 (HUD ATTACH cell empty for live agent) | The Agents table rendered the live Boss row but the ATTACH cell was EMPTY (no clickable button), even though the ttyd attach URL resolved (200). Root cause: `attach_base` lived ONLY on the `/clients` record (per hostname); the `/agents` agent row carried `tmux_target` but NO `attach_base`, and the HUD did not join client→agent, so it had nothing to build the per-row link from. | ✅ Folded into PRODUCT seed: §4 agent record now includes `attach_base`+`attach_url` JOINED server-side (queue-server looks up owning client by host, emits ready-built `attach_url=<attach_base>/?arg=-t&arg=<tmux_target>`); §5.2 says the HUD renders the ATTACH button directly from `attach_url` (never empty for a live heartbeating agent); J8 strengthened — must assert the rendered `/dashboard` HTML carries the clickable `attach_url` anchor in the Boss row, not just that the URL resolves. To re-prove on fresh hydration. |

| CEO 2026-06-18 (nested spawn → ttyd/tmux disconnect) | An engineer born from a TODO comment runs `mp spawn` to create ANOTHER engineer and the parent ttyd/tmux session DROPS (the human watching gets disconnected). Root cause: `mp spawn` clobbers the session on a nested call — either a "kill-session + new-session" idempotency pattern that destroys the running `mc-<sess>` (killing all agents + ttyd), or a bare `tmux new-session`/`switch-client`/`select-window` run from INSIDE the caller's pane ($TMUX set) that yanks/drops the attached client. | ✅ Folded into PRODUCT seed §4 mp-spawn: session created ONCE idempotently (`has-session || new-session`), NEVER kill-session; additional agents use `new-window -d` ONLY; spawn uses detached explicitly-targeted tmux cmds (no switch-client/select-window/attach) with `TMUX=` cleared so the caller's pane is never the implicit target; + self-check. New gate J36: spawn engineer-from-engineer, assert parent session + windows + attached ttyd client all survive (no disconnect) + child appears alive. |

| CEO 2026-06-18 (browser-QA: ATTACH lands on wrong window) | Clicking the Boss attach opened an ENGINEER pane, not the Boss. Root cause: per-connection `tmux attach -t mc-<sess>:<tab>` — all clients on ONE tmux session SHARE the current-window (tmux forces every client to the same active window), so attaching to Boss while an eng window is active lands on the eng pane (and selecting would yank other viewers). | ✅ §5.7 folded: ttyd attach-helper gives each connection its OWN window via a GROUPED session (`new-session -d -t mc-<sess> -s _v_<tab>_$$ ; select-window <tab> ; destroy-unattached on ; attach`) — shares windows, independent current-window. Gate J31(d): Boss attach lands on Boss even when an eng window is active; per-row isolated. |
| CEO 2026-06-18 (browser-QA: card modal stuck hidden) | `#modal{display:block}` while `#modalbg{display:none}` → normal close click failed (force-click worked). Root cause: panel + backdrop toggled by two independent style.display writes that desync. | ✅ §7 folded: modal open/close ATOMIC — single source of truth (one class toggle OR one openModal/closeModal pair setting BOTH); close bound to ✕ + Escape + backdrop, all calling the same closeModal; after close both hidden, no desync. Gate J31(c): close always works with a plain click + Esc + backdrop. |
| CEO 2026-06-18 (browser-QA: proof chip not rendering) | Server proof objects stored `{type,ref}` but client renderer expected `{kind,url,body}` → video chips blank. Root cause: proof object shape wasn't a hard contract (only the `set` task fields were), so server/client diverged. | ✅ §11 folded: PROOF OBJECT SHAPE is now a HARD contract — `proofs[]` elements = `{kind,url,body,ts}` (kind NOT type, url NOT ref), stored verbatim + returned unchanged + renderer reads `p.kind`+`p.url`/`p.body`; `{type,ref}` forbidden. Gate J22: post a video proof → board returns `{kind:"video",url:…}` AND card renders `<video>`. |

| CEO 2026-06-18 (re-QA: grouped-session attach REGRESSED — "can't find session") | My §5.7 grouped-session fix created the view session DETACHED (`new-session -d`) then set `destroy-unattached on` then a separate `attach` → tmux reaped the clientless session INSTANTLY, so `attach` failed with `can't find session: _v_Boss_…` + ttyd "Press Enter to Reconnect" (no recovery). | ✅ Re-fixed §5.7: create the grouped session ATTACHED in ttyd's pty in ONE command (`exec tmux new-session -t mc-<sess> -s _v_<tab>_<uniq> \; select-window -t <tab> \; set-option destroy-unattached on`) — NO `-d`, set destroy-unattached LAST (after attached). Documented the exact regression. J31(d) strengthened: session must persist, no "can't find session"/reconnect. |
| CEO 2026-06-18 (re-QA: proof chip still fails — stored as text) | Uploaded PNG/MP4 were accepted by `/todo/proof` but stored `kind:'text'` → text chips, not image/video. Root cause: server `_proof` did `kind = body.get('kind','text')` — trusted the client + blind-defaulted to text; no media classification. (Renderer was correct.) | ✅ Folded §11: server MUST CLASSIFY kind from the actual media — multipart/blob by content-type/extension, url by extension (.png→image, .mp4→video, else link), text only for a genuine typed note; UI exposes a working proof control. J22 strengthened: upload a real PNG + MP4 via the UI path → assert kind=image/video (NOT text) + `<img>`/`<video>` renders. |

| CEO 2026-06-20 (FEATURE: pin tasks, WhatsApp-starred) | New feature, not a bug. Pin/star tasks to float to the top; pin order = insertion order; MAX 5; unpin restores; persists across reload + restart. | ✅ Folded into seed: §4 store (`pinned`,`pinRank`,`pinSeq` in board.v2.json), §11 ops `pin`/`unpin` (max-5 enforced → `{ok:false,error:"pin_limit"}`, monotonic pinRank), board ordering (pinned-first by pinRank then normal), §7 UI (★ control + distinct pinned group at top), gate J37 + feature F23. Re-hydrating clean to prove one-shot. |

| CEO 2026-06-21 (FEATURE: jump-to-latest in comment thread) | New feature: a floating ↓ button in the card comment thread that appears when scrolled up and smooth-scrolls to the newest comment, hidden at bottom. | ✅ Folded §7.4: floating jump-to-latest control anchored bottom-right of the scrollable thread, toggled by the thread scroll handler (`scrollHeight-scrollTop-clientHeight>threshold`), smooth-scrolls to newest on click, hidden at bottom; thread opens scrolled to bottom. Gate J38 + feature F24. |
